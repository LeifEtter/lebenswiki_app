enum ContentType {
  shortsByCategory,
  packsByCategory,
  shortBookmarks,
  packBookmarks,
  drafts,
  yourShorts,
  comments,
  hardcodePacks,
}

enum MenuType {
  reactShort,
  reactShortComment,
  moreShort,
  commentShort,
}
